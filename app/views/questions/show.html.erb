<p id="notice"><%= notice %></p>

<p>
  <strong>Code:</strong>
  <%= @question.code %>
</p>

<p>
  <strong>Image:</strong>
  <%= link_to @question.image.filename, @question.image if @question.image.attached? %>
</p>

<%= link_to 'Edit', edit_question_path(@question) %> |
<%= link_to 'Back', questions_path %>
